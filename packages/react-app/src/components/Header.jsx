import { PageHeader } from "antd";
import React from "react";

// displays a page header

export default function Header() {
  return <PageHeader title="🌿 Self Serve Mint" subTitle="A simple NFT minting contract that's open to all." />;
}
